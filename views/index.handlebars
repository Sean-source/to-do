<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <ol>
                {{#each actionitems}}
                {{#unless complete}}
                <li>
                    <h3>{{name}}</h3>
                    <h4>{{description}}</h4>

                </li>
                {{/unless}}
                {{/each}}
            </ol>
        </div>
        <div class="col-sm-4">
            <input type="text" id="items">
            <button id="add-item">Add new to-do</button>
        </div>
        <div class="col-sm-4">
            <ol>
                {{#each actionitems}}
                {{#if complete}}
                <li>
                    <h3>{{name}}</h3>
                    <h4>{{description}}</h4>
                </li>
                {{/if}}
                {{/each}}
            </ol>
        </div>
    </div>
</div>