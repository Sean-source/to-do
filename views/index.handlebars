<div class="container">

    <div class="jumbotron">
           <svg class="bi bi-check-circle" width="1em" height="1em" viewBox="0 0 16 16" fill="currentColor"
            xmlns="http://www.w3.org/2000/svg">
            <path fill-rule="evenodd"
                d="M15.354 2.646a.5.5 0 010 .708l-7 7a.5.5 0 01-.708 0l-3-3a.5.5 0 11.708-.708L8 9.293l6.646-6.647a.5.5 0 01.708 0z"
                clip-rule="evenodd" />
            <path fill-rule="evenodd"
                d="M8 2.5A5.5 5.5 0 1013.5 8a.5.5 0 011 0 6.5 6.5 0 11-3.25-5.63.5.5 0 11-.5.865A5.472 5.472 0 008 2.5z"
                clip-rule="evenodd" />
        </svg>
        <h1>To-Do List</h1>
     
    </div>


</div>
<div class="container">
    <div class="row">

        <div class="col-sm-4">
            <h4>Add new</h4>
            <br>

            <h6>Title</h6>
            <input type="text" id="items">
            <br>
            <br>
            <h6>Description</h6>
            <textarea name="" id="description" cols="30" rows="10"></textarea>
            <button class="btn btn-primary" id="add-item">Add new to-do</button>

        </div>

        <div class="col-sm-4">
            <h4>Pending Items</h4>
            <ol>
                <br>
                {{#each actionitems}}
                {{#unless complete}}


                <li>
                    <h6>{{name}}</h6>
                    <p>{{description}}</p>
                    <button class="btn btn-primary update-item" data-id="{{id}}" data-complete="{{complete}}">Task
                        Complete</button>
                </li>

                <hr>

                {{/unless}}
                {{/each}}
            </ol>
        </div>

        <div class="col-sm-4">
            <h4>Completed Items</h4>
            <ol>
                {{#each actionitems}}
                {{#if complete}}
                <li>
                    <h6>{{name}}</h6>
                    <p>{{description}}</p>
                </li>
                {{/if}}
                {{/each}}
            </ol>
        </div>
    </div>
</div>